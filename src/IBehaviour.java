public interface IBehaviour
{
    public int moveCommand();
}
//une interface est une classe abstraite à 100%, il n'y a donc que de methodes abstraites dans l'interface
//une classe abstraite est une classe contenant au moins une méthode abstraite
//une interface est plus flexible que la classe abstraite

